# AI Smart Academic Project Evaluation System (ASPES)

<div align="center">
  <h1>🎓 ASPES</h1>
  <h3>AI Smart Academic Project Evaluation System</h3>
  <p>
    <img src="https://img.shields.io/badge/FastAPI-0.111-009688?style=flat-square&logo=fastapi" alt="FastAPI">
    <img src="https://img.shields.io/badge/React-18.2-61DAFB?style=flat-square&logo=react" alt="React">
    <img src="https://img.shields.io/badge/Python-3.11+-3776AB?style=flat-square&logo=python" alt="Python">
    <img src="https://img.shields.io/badge/PostgreSQL-15+-336791?style=flat-square&logo=postgresql" alt="PostgreSQL">
    <img src="https://img.shields.io/badge/PyTorch-2.3-EE4C2C?style=flat-square&logo=pytorch" alt="PyTorch">
    <img src="https://img.shields.io/badge/License-MIT-green?style=flat-square" alt="License">
  </p>
  <p>An AI-powered system for evaluating academic software projects with automated code analysis, plagiarism detection, AI-generated code detection, and comprehensive feedback generation.</p>
</div>

---

## 📋 Table of Contents

- [Features](#-features)
- [Tech Stack](#-tech-stack)
- [Architecture](#-architecture)
- [Prerequisites](#-prerequisites)
- [Quick Start (Docker)](#-quick-start-docker)
- [Manual Setup](#-manual-setup)
  - [Backend Setup](#backend-setup)
  - [Frontend Setup](#frontend-setup)
- [Environment Variables](#-environment-variables)
- [Project Structure](#-project-structure)
- [API Documentation](#-api-documentation)
- [Running Tests](#-running-tests)
- [Deployment](#-deployment)
- [Contributing](#-contributing)

---

## ✨ Features

| Feature | Description |
|---|---|
| 🔍 **Code Quality Analysis** | Automated analysis using Radon (cyclomatic complexity, maintainability index) |
| 🤖 **AI Code Detection** | Detect AI-generated code using Sentence-BERT semantic analysis |
| 📄 **Documentation Evaluation** | NLP-powered assessment of project reports and documentation |
| 🔗 **Report-Code Alignment** | Verify that code implementation matches the submitted report |
| 🕵️ **Plagiarism Detection** | Semantic similarity-based cross-submission plagiarism analysis |
| 💬 **GPT-4 Feedback** | Personalized, detailed feedback generated by GPT-4 |
| 📊 **Comprehensive Scoring** | Multi-dimensional scoring with weighted rubrics |
| ⚡ **Async Processing** | Celery-powered background AI evaluation tasks |
| 🔐 **Role-Based Access** | Professor, Student, and Admin roles |
| 📈 **Real-time Progress** | WebSocket-based live evaluation progress updates |

---

## 🛠 Tech Stack

### Backend
- **Framework**: FastAPI 0.111 (async Python web framework)
- **Database**: PostgreSQL 15+ with SQLAlchemy 2.0 (async ORM)
- **Migrations**: Alembic
- **Authentication**: JWT tokens (python-jose) + bcrypt password hashing
- **Task Queue**: Celery 5.4 + Redis 7
- **AI/ML**: PyTorch 2.3, HuggingFace Transformers, Sentence-BERT
- **NLP**: spaCy, NLTK
- **Code Analysis**: Radon, Pylint
- **Document Parsing**: python-docx, pypdf, pdfplumber

### Frontend
- **Framework**: React 18.2 (with hooks)
- **Routing**: React Router v6
- **State**: Redux Toolkit + React Query
- **Styling**: Tailwind CSS 3.4
- **Charts**: Recharts + Nivo
- **Forms**: React Hook Form + Yup

### Infrastructure
- **Containerization**: Docker + Docker Compose
- **Cache**: Redis 7
- **Task Monitoring**: Flower dashboard
- **Logging**: Loguru + Structlog

---

## 🏗 Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                        ASPES Architecture                         │
├─────────────┬──────────────┬──────────────┬──────────────────────┤
│   React     │   FastAPI    │   Celery     │   AI Engine          │
│  Frontend   │   Backend    │   Workers    │                      │
│  :3000      │   :8000      │   (async)    │  ┌────────────────┐  │
│             │              │              │  │ code_analyzer   │  │
│  Dashboard  │  REST API    │  Evaluation  │  │ ai_code_detect │  │
│  Submission │  WebSockets  │  Tasks       │  │ doc_evaluator  │  │
│  Results    │  Auth        │              │  │ plagiarism_det │  │
│             │              │              │  │ feedback_gen   │  │
├─────────────┴──────┬───────┴──────┬───────┴──┴────────────────┘  │
│                    │              │                               │
│              PostgreSQL 15    Redis 7                             │
│              (Primary DB)   (Cache +                             │
│                              Task Queue)                         │
└────────────────────────────────────────────────────────────────┘
```

---

## 📦 Prerequisites

| Tool | Version | Purpose |
|---|---|---|
| Python | 3.11+ | Backend runtime |
| Node.js | 18+ | Frontend runtime |
| PostgreSQL | 15+ | Primary database |
| Redis | 7+ | Cache & task queue |
| Docker | 24+ | Optional (containerization) |
| Docker Compose | 2.x | Optional (multi-service) |

---

## 🚀 Quick Start (Docker)

The fastest way to run the entire system:

```bash
# 1. Clone the repository
git clone https://github.com/your-org/aspes.git
cd aspes

# 2. Copy environment files
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env

# 3. Edit backend/.env with your actual API keys
#    (at minimum: SECRET_KEY and OPENAI_API_KEY)
nano backend/.env

# 4. Run all services
docker-compose up --build

# 5. In a new terminal, run database migrations
docker-compose exec backend alembic upgrade head

# Services available:
# Frontend:    http://localhost:3000
# Backend API: http://localhost:8000
# API Docs:    http://localhost:8000/docs
# Flower:      http://localhost:5555  (admin:aspes_flower_password)
```

---

## 🔧 Manual Setup

### Backend Setup

```bash
cd backend

# 1. Create and activate virtual environment
python -m venv venv

# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 2. Install dependencies
pip install -r requirements.txt

# 3. Download spaCy English model
python -m spacy download en_core_web_sm

# 4. Download NLTK data
python -c "import nltk; nltk.download('punkt'); nltk.download('stopwords')"

# 5. Copy and configure environment
cp .env.example .env
# Edit .env with your database URL, Redis URL, API keys

# 6. Run database migrations
alembic upgrade head

# 7. Start the development server
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 8. (Optional) Start Celery worker in a separate terminal
celery -A app.tasks.celery_app worker --loglevel=info
```

### Frontend Setup

```bash
cd frontend

# 1. Install dependencies
npm install

# 2. Copy environment file
cp .env.example .env

# 3. Start development server
npm start

# App: http://localhost:3000
```

---

## 🔐 Environment Variables

### Backend (backend/.env)

| Variable | Required | Description |
|---|---|---|
| `SECRET_KEY` | ✅ | JWT signing key (min 32 chars) |
| `DATABASE_URL` | ✅ | PostgreSQL connection string |
| `REDIS_URL` | ✅ | Redis connection string |
| `OPENAI_API_KEY` | ✅ | GPT-4 API key for feedback |
| `ALLOWED_ORIGINS` | ✅ | Comma-separated CORS origins |
| `SBERT_MODEL_NAME` | ⬜ | Sentence-BERT model (default: all-MiniLM-L6-v2) |
| `MAX_UPLOAD_SIZE` | ⬜ | Max file size in bytes (default: 50MB) |

### Frontend (frontend/.env)

| Variable | Required | Description |
|---|---|---|
| `REACT_APP_API_URL` | ✅ | Backend API base URL |
| `REACT_APP_WS_URL` | ✅ | WebSocket URL for real-time updates |

---

## 📁 Project Structure

```
ASPES - 2/
├── backend/
│   ├── app/
│   │   ├── main.py              ← FastAPI application entry point
│   │   ├── config.py            ← Settings & environment variables
│   │   ├── api/                 ← API route handlers
│   │   │   ├── auth.py          ← Authentication endpoints
│   │   │   ├── projects.py      ← Project CRUD endpoints
│   │   │   ├── evaluations.py   ← Evaluation trigger & results
│   │   │   └── users.py         ← User management
│   │   ├── models/              ← SQLAlchemy database models
│   │   ├── schemas/             ← Pydantic request/response schemas
│   │   ├── services/            ← Business logic layer
│   │   ├── ai_engine/           ← AI evaluation modules
│   │   │   ├── code_analyzer.py        ← Code quality metrics
│   │   │   ├── ai_code_detector.py     ← AI-generated code detection
│   │   │   ├── doc_evaluator.py        ← Documentation quality
│   │   │   ├── report_code_aligner.py  ← Report ↔ code consistency
│   │   │   ├── plagiarism_detector.py  ← Cross-submission plagiarism
│   │   │   ├── feedback_generator.py   ← GPT-4 feedback
│   │   │   └── comprehensive_scorer.py ← Final score aggregation
│   │   ├── utils/               ← Shared utilities
│   │   ├── database/            ← DB connection & session management
│   │   └── tasks/               ← Celery background tasks
│   ├── tests/                   ← Backend test suite
│   ├── uploads/                 ← Uploaded project files (git-ignored)
│   ├── .env.example             ← Environment template
│   ├── requirements.txt         ← Python dependencies
│   └── README.md
│
├── frontend/
│   ├── public/                  ← Static assets
│   └── src/
│       ├── components/          ← Reusable React components
│       │   ├── Auth/            ← Login, Register, ProtectedRoute
│       │   ├── Dashboard/       ← Dashboard widgets & layout
│       │   ├── Project/         ← Project submission & listing
│       │   ├── Evaluation/      ← Evaluation results & charts
│       │   └── Common/          ← Shared UI elements
│       ├── pages/               ← Top-level route pages
│       ├── services/            ← Axios API service layer
│       ├── context/             ← React context providers
│       ├── hooks/               ← Custom React hooks
│       ├── utils/               ← Helper functions
│       ├── App.jsx              ← Root component + routing
│       └── index.js             ← React entry point
│
├── docker-compose.yml           ← Multi-service Docker setup
└── README.md                    ← This file
```

---

## 📖 API Documentation

Once the backend is running, interactive API docs are available at:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **OpenAPI JSON**: http://localhost:8000/openapi.json

---

## 🧪 Running Tests

```bash
# Backend tests
cd backend
pytest tests/ -v --cov=app --cov-report=html

# Frontend tests
cd frontend
npm test

# Frontend coverage
npm run test:coverage
```

---

## 🐳 Deployment

For production deployment:

```bash
# Build production images
docker-compose -f docker-compose.yml -f docker-compose.prod.yml build

# Run in production mode
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

---

## 🛠️ Troubleshooting Guide

### Backend Issues
- **Database Connection Error**: Ensure the PostgreSQL container is healthy (`docker-compose ps`). Check if the `DATABASE_URL` in `.env` matches the container service name (`db`).
- **Celery Worker Failures**: If the worker isn't picking up tasks, check Redis connectivity. Running `celery -A app.tasks.evaluation_tasks.celery_app inspect ping` can verify connectivity.
- **NLP Model Loading**: The first run of the worker downloads SBERT models. This may take a few minutes. Ensure you have internet access or pre-mount the volumes.

### Frontend Issues
- **API Unreachable**: Ensure `REACT_APP_API_URL` in `frontend/.env` is set correctly. If running in Docker, it should usually be `http://localhost:8000/api/v1`.
- **Blank Screen**: Check the browser console for JavaScript errors. Common causes include failed React Query fetches or 401 Unauthorized loops.

## 📄 License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

---

**ASPES Project Status**: ✅ v1.0.0 Stable Release
*Developed with ❤️ and AI for Academic Excellence.*
